<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.trabalhocs">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data android:name="DATABASE" android:value="componente_vendas.db" />
        <meta-data android:name="VERSION" android:value="2" />
        <meta-data android:name="QUERY_LOG" android:value="true" />
        <meta-data android:name="DOMAIN_PACKAGE_NAME" android:value="com.example.trabalhocs" />

        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".View.GerenciarReceita"
            android:label="@string/title_activity_gerenciar_receita"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".View.GerenciarGrupo"
            android:label="@string/title_activity_gerenciargrupo"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".View.CadastrarRecursoActivity" />
        <activity android:name=".View.CadastrarProdutoActivity" />
        <activity android:name=".View.RegistrarVendaActivity" />
        <activity android:name=".View.EstoqueActivity" />
        <activity android:name=".View.RegistrarCompraActivity" />
        <activity android:name=".View.InventarioActivity" />
        <activity android:name=".View.CadastrarModeloProdutoActivity"
            android:windowSoftInputMode="adjustPan"/>
        <activity android:name=".View.FabricarProdutoActivity" />
        <activity android:name=".View.HistoricoActivity" />
        <activity android:name=".View.ConfiguracoesActivity" />
    </application>

</manifest>